{
    "$schema": "https://docs.renovatebot.com/renovate-schema.json",
    "baseBranches": ["main"],
    "labels": ["dependencies"],
    "packageRules": [
        {
            "matchPaths": ["+(package.json)"],
            "automerge": true
        }
    ]
}